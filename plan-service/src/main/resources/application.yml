server:
  port: 8082

spring:
  application:
    name: plan-service   # Registered name in Eureka

  datasource:
    url: jdbc:postgresql://localhost:5432/plandb
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
